% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{prepare_gene_intensities}
\alias{prepare_gene_intensities}
\title{Prepare gene intensities}
\usage{
prepare_gene_intensities(
  norm = FALSE,
  log = FALSE,
  series_matrix_file = NULL,
  platform_file = NULL,
  probe_col = "ID",
  symbol_col = "Gene.Symbol",
  entrez_col = "ENTREZ_GENE_ID",
  series_skip = NULL,
  platform_skip = NULL,
  series_sep = "\\t",
  platform_sep = "\\t",
  trim_cols = FALSE,
  numeric_cols = NULL,
  gene_synonyms = FALSE
)
}
\arguments{
\item{norm}{Logical. If TRUE, the expression data will be quantile-normalized. Default is FALSE.}

\item{log}{Logical. If TRUE, the method will log2-transform the data if necessary. Default FALSE}

\item{series_matrix_file}{Character. Path to the raw series matrix file. Optional if the matrix is already in `gene_intensities`.}

\item{platform_file}{Character. Path to the platform file mapping probes to gene symbols and Entrez IDs. Required for mapping.}

\item{probe_col}{Character. Column name in the platform file containing probe IDs. Default is "ID".}

\item{symbol_col}{Character. Column name in the platform file containing gene symbols. Default is "Gene.Symbol".}

\item{entrez_col}{Character. Column name in the platform file containing Entrez IDs. Default is "ENTREZ_GENE_ID".}

\item{series_skip}{Integer. Number of lines to skip at the start of the series matrix file.}

\item{platform_skip}{Integer. Number of lines to skip at the start of the platform file.}

\item{series_sep}{Character. Field separator in the series matrix file. Default is tab ("\t").}

\item{platform_sep}{Character. Field separator in the platform file. Default is tab ("\t").}

\item{trim_cols}{Character vector. Column names in the platform to trim whitespace from. Default is NULL.}

\item{numeric_cols}{Character vector. Column names in the platform to convert to numeric. Default is NULL.}

\item{gene_synonyms}{Logical. If TRUE, the synonyms are handled. Default FALSE}
}
\value{
Data frame of cleaned gene intensities with Entrez IDs as rownames
}
\description{
Cleans a raw gene expression matrix, handles multiple probes mapping to the same gene, ensures data are normalized and log-transformed, and maps gene symbols to Entrez IDs.
}
